---
description: 轻阅必须兼顾手机/平板/PC 三形态——方案、实现、验收均不得只做单端
alwaysApply: true
---

# 三形态必顾（手机 / 平板 / PC）

## 强制要求

- **任何方案、实现、重构、缺陷修复、交互与动效改动，必须同时兼顾三形态**：手机（phone）、平板（tablet）、PC（desktop）。
- **禁止**只按「手机」或只按「桌面」设计后默认其它端可用。
- 交付说明与自测至少覆盖三形态各一档（或说明为何某形态不适用且已显式降级）。

## 形态定义（与代码一致）

- 使用现有 `DeviceClass` / `detectDevice()`：`width < 768` → phone；`< 1100` → tablet；否则 desktop（`DUAL_COLUMN_MIN_WIDTH`）。
- **形态（宽度）与指针（fine/coarse/hover）正交**：平板常为「中等宽度 + 触控」；触控笔记本可能是「desktop 宽 + coarse」。策略必须两维都看，禁止只用 `desktopUi` 或 `isMobile` 一刀切。

## 设计与实现约束

- 阅读手势、翻页、热区、chrome、双栏、动效：走**同一管道**，用 profile/参数按形态×指针调档，禁止复制三套互斥逻辑。
- EPUB 等 iframe 内容：三形态都要能把必要指针/点击桥到壳层；不得假设「只有桌面才点热区」。
- 禁止对 EPUB **活页**做会破坏分栏的 transform；动效优先 ghost-only，按形态可 full / lite 降级。
- 双栏仅 `width >= 1100` 且非 scroll；平板未达宽不得当 PC 开双栏。
- 与 `qingyue-constraints` 一致：主交付仍是 Web/PWA；本规则管**视口形态体验**，不等于擅自上原生壳。

## 验收底线

改动涉及阅读器/手势/翻页/布局时，验收表至少含：

| 形态 | 必查 |
|------|------|
| 手机 | 触控滑/点、系统手势不误伤、小屏 chrome 显隐后版面 |
| 平板 | 竖横屏、触控为主、外接指针时滚轮可选 |
| PC | 键鼠/滚轮/热区 hover、宽屏双栏与动效 |

只验证一端视为未完成。
