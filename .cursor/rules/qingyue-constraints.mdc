---
description: 轻阅（qingyue）产品与工程约束——本地优先、合规工具、跨端预留、禁止擅自装重型工具链
alwaysApply: true
---

# 轻阅项目约束

## 产品定位

- 本地文档 / 电子书阅读 PWA（Vue 3 + Vite + TypeScript）。
- 支持 EPUB / TXT / PDF；数据默认仅存本机（IndexedDB / 关联文件夹）。
- 使用场景：国内个人自用；不做成公开分享或内容分发站。

## 合规与工具安装

- **只使用可合法获取的开源 / 官方工具**（npm 官方或可信镜像、GitHub/官方文档渠道）。
- **禁止**引导或执行：破解软件、来路不明安装包、盗版组件、未授权商用字体/素材入库发行。
- **禁止擅自安装重型原生工具链**：尤其 **Rust / rustup、MSVC 完整套件、Tauri CLI**，除非用户在当轮对话中明确要求。
- 依赖优先选许可证清晰（MIT / Apache-2.0 等）且可 npm 安装的包；新增依赖说明用途，避免冗余。
- 用户导入的书籍版权由用户自负；产品侧不做未授权内容分发能力。

## 跨端策略（当前）

- **主交付面：Web / PWA**（浏览器 + 可安装 PWA）。
- 平台差异必须走 `src/platform`（`files` / `tts` / `clipboard` / `env`）；业务与 Store **禁止**直接调用 `showDirectoryPicker`、`speechSynthesis`、`navigator.clipboard` 等浏览器 API。
- 桌面壳：**暂不推进 Tauri**（依赖 Rust）。若将来需要桌面封装，优先评估 **Electron 官方开源方案**，且须用户明确同意后再接入。
- 移动壳（Capacitor 等）未启动前，不要提前大改移动专有 UI；预留 `PlatformKind` 即可。
- 书库语义分端：Web/桌面可为「IDB 小文件 + 文件夹引用」；移动将来为「导入到应用沙箱」——**不要强行三端同一套「关联文件夹」流程**。

## 数据与同步

- **当前不做云同步**；各端本地独立。
- 写操作经 `src/repos/*`（books / annotations / progress / settings / stats），便于以后挂同步，**禁止**在 View 里直接 `db.*.put/delete` 散落写入。
- 元数据与书文件分离；同步若后期做，优先进度/批注/设置，大文件另议。
- 可保留字段 `updatedAt` / `deletedAt` / `remoteId`，但**不要实现**登录、冲突 UI、同步引擎，除非用户明确要求。

## 架构边界

- 阅读渲染：继续 Web 引擎（epubjs / pdfjs / 自研 TXT）；经 `createEngine` / `ReaderEngine`，不要在 View 里 `new XxxEngine()`。
- 阅读器 UI：面板与会话逻辑放 `composables` + `components/reader/*`；`ReaderView` 只做编排，避免重新堆成上帝组件。
- 新增本地能力（文件、TTS、剪贴板、分享）先扩展 Platform 接口，再写 Web 实现。

## 部署与范围

- GitHub Pages / 静态托管保持 `base` 与 PWA 缓存约定；不要引入必须后端的运行时。
- 不擅自加账号系统、分析 SDK、第三方上传，除非用户明确要求。
- 不主动跑长时间下载/静默安装系统级软件；需要安装时先说明用途并征得同意。

## 与用户确认后再做的事项

- 安装 Rust / 初始化 Tauri 或 Electron
- 云同步、登录、多端数据合并
- 小程序或非 WebView 重写渲染内核
